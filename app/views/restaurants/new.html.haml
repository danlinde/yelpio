%div
	%h2 Enter requested information to Create Restaurant:

= form_for @restaurant do |f|
	= f.label :name
	= f.text_field :name, placeholder: 'name'
	= f.label :description
	= f.text_area :description, placeholder: 'enter a description of the restaurant here...'
	= f.submit